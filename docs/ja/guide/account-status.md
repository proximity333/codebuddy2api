# アカウント状態

アカウント状態では各 CodeBuddy アカウントの使用量、チェックイン、モデルを確認します。

1. 「すべて更新」を押すか、個別アカウントの「更新」を押します。
2. プラン、使用済み / 合計、残りの割合、「リセット時刻」を確認します。
3. チェックイン状態を確認し、可能な場合は「チェックイン」または「すべてチェックイン」を押します。
4. API テストやクライアントで選ぶ前に「モデル（N）」を展開します。

各モデルには上流カタログの情報が表示されます。表示名とモデル ID（クリックでコピー）、倍率（例 `x3.33`）、説明、「エンタープライズ / 内部モデル / 無料 / デフォルト」バッジに加え、実施中のキャンペーンがあれば「期間限定無料」などのバッジと割引後の倍率（`x3.33 → x0.00`）です。その下にコンテキスト長、出力上限、画像 / ツール / 推論の対応状況と「思考のみ」、さらに上流が宣言した残りの情報——ベンダー、リクエスト上限、選択可能なコンテキスト長、デフォルトおよび選択可能な思考強度、バリアント ID、機能タグ、キャンペーンの説明と終了日——が続きます。上流が宣言していない項目はカードに表示されません。機能タグ、選択可能なコンテキスト長、思考強度、バリアントはそれぞれ先頭 8 件まで保持し、キャンペーンは期間終了後に表示されなくなります。最初の 8 件のみ表示し、「すべて表示（N）」で全件を展開します。展開後のボタンは「折りたたむ」に変わり、モデルが 8 件以下の場合はこのボタン自体が表示されません。

説明は上流が中国語と英語のみを提供しているため、日本語画面では英語の説明が表示されます。

カタログは上流 `/v3/config` から取得し、初回取得時に認証情報へキャッシュされるため、以降のページ読み込みでは上流へ問いません。カードの「更新」（または「すべて更新」）は上流へ再度問い、返ってきたカタログでキャッシュを置き換えます。更新が失敗した場合や上流が空を返した場合は、従来のキャッシュを保持します。「認証情報」でモデル一覧を手動編集した場合、その編集で削除されたモデルのメタデータも破棄されます。更新が置き換えるのはカタログだけで、「認証情報」で管理するモデル許可リスト（ゲートウェイのルーティングに使われるもの）は変更しません。

使用量が不明、または確認に失敗した場合は「認証情報」を確認し、「Debug」を開きます。

## 自動チェックイン

各アカウントのチェックイン行の下に「自動チェックイン」のスイッチがあります（既定はオフ）。
オンにして時刻を選ぶと、ゲートウェイがその時刻に 1 日 1 回そのアカウントをチェックインします。

時刻は**サーバーのローカルタイムゾーン**で解釈されます（アプリの他の部分と同様で、`TZ` 環境変数で指定できます）。
スケジューラはゲートウェイのプロセス内で動作するため、ゲートウェイの稼働中のみ発火します。リクエストの合間に停止する構成では、次回のポーリング時に実行されます。
すでにチェックイン済みのアカウントはスキップされ、成功した場合のみ記録されるため、失敗した場合は翌日まで待たずに再試行されます。
